const moxios = require("moxios");

describe('get secret action creator', () => {
   beforeEach(() => {
      moxios.install();
   })
   afterEach(() => {
      moxios.uninstall();
   })
   test('adds response word to state', () => {

   })

});